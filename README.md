# docker-redash

## how to run

- init database

```code
docker-compose run --rm redash-server create_db
```

- start servers

```code
docker-compose up -d
```

- config server

```code
http://localhost
```